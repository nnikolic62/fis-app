/**
 * Tailwind Config Package
 * 
 * This package provides shared Tailwind CSS v4 theme configuration.
 * Import the theme CSS in your app:
 * 
 * @example
 * ```css
 * @import "@repo/tailwind-config/theme.css";
 * ```
 */

// This file exists to satisfy TypeScript config requirements in the monorepo.
// The main export is the CSS theme file, not TypeScript code.

export {};

